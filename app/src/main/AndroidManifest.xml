<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thinkdevs.designmymfcommon" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activitymain.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activitycashaccounts.CashAccountsActivity"
            android:label="@string/act_name_cash_accounts" >
        </activity>
        <activity
            android:name=".activitycashaccounts.CashAccountsRecyclerViewActivity"
            android:label="@string/act_name_cash_accounts" >
        </activity>

    </application>

</manifest>
